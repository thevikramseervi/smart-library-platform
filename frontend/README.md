# Smart Library Platform — Frontend

React + TypeScript + Vite client for the Smart Library Platform API.

## Local development

```bash
pnpm install
cp .env.example .env
pnpm dev
```

The app runs at `http://localhost:5173` and expects the backend at `VITE_API_BASE_URL`.
